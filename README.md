# Portfolio Landing (HTML)

This folder contains a simple static landing page that can be published via GitHub Pages.

## Publish (manual)
1) Create a repo named `albertquerol12345.github.io` (public)
2) Copy `index.html` + `styles.css` to the repo root
3) Push to `main`
4) GitHub Pages will serve: https://albertquerol12345.github.io

